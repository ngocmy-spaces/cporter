{
  "name": "@cporter/mcp",
  "version": "0.1.0",
  "description": "Model Context Protocol server for cPorter — lets AI agents deploy, check status, and roll back.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "cporter-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@cporter/sdk": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
