# Base URL of the cPorter API. Leave unset to use same-origin `/api/v1`
# (Laravel serves the SPA in prod; Vite proxies /api → localhost:8000 in dev).
# VITE_API_BASE_URL=/api/v1

# Dev-only: where Vite proxies /api during `pnpm dev`.
# VITE_API_PROXY_TARGET=http://localhost:8000
